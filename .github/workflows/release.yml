name: Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  build:
    name: Build ${{ matrix.target }}
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        include:
          - os: macos-latest
            target: x86_64-apple-darwin
            binary: ctk
          - os: macos-latest
            target: aarch64-apple-darwin
            binary: ctk
          - os: windows-latest
            target: x86_64-pc-windows-msvc
            binary: ctk.exe

    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - name: Build
        run: cargo build --release --target ${{ matrix.target }}

      - name: Package
        shell: bash
        run: |
          cd target/${{ matrix.target }}/release
          if [ "${{ matrix.os }}" = "windows-latest" ]; then
            7z a ../../../ctk-${{ matrix.target }}.zip ${{ matrix.binary }}
          else
            tar czf ../../../ctk-${{ matrix.target }}.tar.gz ${{ matrix.binary }}
          fi

      - name: Upload artifact
        uses: actions/upload-artifact@65c4c4a1ddee5b72f698fdd19549f0f0fb45cf08 # v4.6.0
        with:
          name: ctk-${{ matrix.target }}
          path: ctk-${{ matrix.target }}.*

  checksums:
    name: Compute SHA256SUMS
    needs: build
    runs-on: ubuntu-latest
    outputs:
      source_sha256: ${{ steps.source.outputs.sha256 }}
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2

      - name: Download artifacts
        uses: actions/download-artifact@fa0a91b85d4f404e444306234669278d213e9fde # v4.1.8
        with:
          path: artifacts

      - name: Compute SHA256SUMS for binaries
        run: |
          find artifacts -type f | sort | xargs sha256sum > SHA256SUMS
          cat SHA256SUMS

      - name: Compute source tarball SHA256
        id: source
        run: |
          TAG=${GITHUB_REF_NAME}
          URL="https://github.com/jackymint/cliTokenKill/archive/refs/tags/${TAG}.tar.gz"
          SHA=$(curl -fsSL "$URL" | sha256sum | awk '{print $1}')
          echo "sha256=${SHA}" >> "$GITHUB_OUTPUT"

      - name: Update Homebrew formula SHA
        run: |
          TAG=${GITHUB_REF_NAME}
          SHA=${{ steps.source.outputs.sha256 }}
          sed -i "s/sha256 \".*\"/sha256 \"${SHA}\"/" Formula/ctk.rb
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add Formula/ctk.rb
          git diff --cached --quiet || git commit -m "Release ${TAG}: update Homebrew formula SHA256"
          git push origin HEAD:main

      - name: Upload artifact
        uses: actions/upload-artifact@65c4c4a1ddee5b72f698fdd19549f0f0fb45cf08 # v4.6.0
        with:
          name: SHA256SUMS
          path: SHA256SUMS

  release:
    name: Create Release
    needs: checksums
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2

      - name: Download artifacts
        uses: actions/download-artifact@fa0a91b85d4f404e444306234669278d213e9fde # v4.1.8
        with:
          path: artifacts

      - name: Create Release
        uses: softprops/action-gh-release@c95fe1489396fe8a9eb87c0abf8aa5b2ef267fda # v2.2.1
        with:
          files: artifacts/**/*
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
